import { CatalogueManagerContent } from './CatalogueManagerContent';

export const metadata = {
  title: 'Try On Library | Ai Vastra',
};

export default function CatalogueManagerPage() {
  return <CatalogueManagerContent />;
}
